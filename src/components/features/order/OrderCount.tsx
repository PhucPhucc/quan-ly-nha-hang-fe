import TitleCount from "@/components/shared/TitleCount";
import { UI_TEXT } from "@/lib/UI_Text";

const OrderCount = () => {
  return <TitleCount text={UI_TEXT.ORDER.TITLE} />;
};

export default OrderCount;
